#include <iostream>
using namespace std;
int map[1515][1515];
int A[1515][1515];
int B[1515][1515];
int input[2];

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	int r, c;
	cin >> r >> c;
	for (int i = 0; i < r; i++) {
		for (int j = 0; j < c; j++) {
			cin >> input[0] >> input[1];
			
		}
	}

	return 0;
}